<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Super Admin Dashboard</title>
  <style>
    /* General Reset */
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body { 
      font-family: 'Arial', sans-serif;
      line-height: 1.6;
      background-color: #eef1f7;
      color: #2c3e50;
      display: flex;
      flex-direction: column;
      min-height: 100vh;
    }

    h1, h2 {
      text-align: center;
      margin: 20px 0;
      color: #283593;
    }

    a {
      text-decoration: none;
      color: #3949ab;
      transition: color 0.3s ease-in-out;
    }

    a:hover {
      color: #f57c00;
    }

    ul {
      list-style: none;
      padding: 0;
      display: flex;
      flex-direction: column;
      gap: 15px;
      max-width: 400px;
      margin: 30px auto;
    }

    ul li {
      background: #fff;
      border: 1px solid #ddd;
      border-radius: 8px;
      text-align: center;
      padding: 15px;
      box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
      transition: transform 0.2s ease-in-out, box-shadow 0.2s ease-in-out;
    }

    ul li:hover {
      transform: translateY(-5px);
      box-shadow: 0 6px 12px rgba(0, 0, 0, 0.2);
    }

    ul li a {
      font-weight: bold;
      color: #3949ab;
      font-size: 1rem;
    }

    ul li a:hover {
      color: #f57c00;
    }

    /* Footer */
    footer {
      background-color: #283593;
      color: white;
      text-align: center;
      padding: 15px;
      font-size: 0.9rem;
      margin-top: auto;
      width: 100%;
    }

    footer a {
      color: #f39c12;
      text-decoration: underline;
    }

    footer a:hover {
      color: #e67e22;
    }

    /* Responsive Design */
    @media (max-width: 768px) {
      ul {
        max-width: 90%;
        gap: 10px;
      }

      ul li {
        padding: 10px;
      }

      ul li a {
        font-size: 0.9rem;
      }
    }
  </style>
</head>
<body>
  <!-- Include Header -->
  <%- include('partials/header') %>

  <h1>Welcome, <%= schoolname.name %>!</h1>
  <h2>Super Admin Dashboard</h2>
  
  <!-- Dashboard Links -->
  <ul>
    <li><a href="/view-requests">View Registration Requests</a></li>
    <li><a href="/super-admin">Manage Users</a></li>
  </ul>

  <%- include('partials/footer') %>
  <!-- Example JavaScript -->
  <script>
    // Display school name in an alert for testing purposes
    alert("Welcome to " + "<%= schoolname.name %>!");
  </script>
</body>
</html>
